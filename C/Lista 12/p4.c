#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    int *inteiro;
    float *real;
    char *caractere;

    inteiro = (int*)malloc(sizeof(int));
    real = (float*)malloc(sizeof(float));
    caractere = (char*)malloc(sizeof(char));

    
}